apiVersion: v1
kind: Service
metadata:
  name: {{ include "uploads.fullname" . }}
  labels:
    {{- include "uploads.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uploads.selectorLabels" . | nindent 4 }}
