apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prisma-migrate-job.fullname" . }}-test-connection"
  labels:
    {{- include "prisma-migrate-job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prisma-migrate-job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
