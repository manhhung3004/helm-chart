apiVersion: v1
kind: Service
metadata:
  name: {{ include "prisma-migrate-job.fullname" . }}
  labels:
    {{- include "prisma-migrate-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prisma-migrate-job.selectorLabels" . | nindent 4 }}
